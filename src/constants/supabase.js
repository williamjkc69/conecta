/**
 * Supabase Configuration Constants
 */

// Supabase Tables
export const TABLES = {
  APPLICATIONS: 'applications',
  JOBS: 'jobs',
  USERS: 'users',
};

// Supabase Buckets
export const BUCKETS = {
  RECORDINGS: 'recordings',
  AVATARS: 'avatars',
};
